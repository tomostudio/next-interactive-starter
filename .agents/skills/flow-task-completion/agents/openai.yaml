interface:
  display_name: "Task Completion"
  short_description: "Selesaikan task delivery dari ticket sampai PR"
  default_prompt: "Use $flow-task-completion to execute a delivery ticket end-to-end, from branch setup through implementation, testing, and PR updates."
policy:
  allow_implicit_invocation: true
