interface:
  display_name: "API Code Review"
  short_description: "Review kode backend dengan standar senior lead yang tegas"
  default_prompt: "Use $api-code-review to review backend changes in this repository with a strict senior-lead mindset."
policy:
  allow_implicit_invocation: true
