interface:
  display_name: "Session Start"
  short_description: "Onboarding sesi repo dari perintah Mulai atau Start"
  default_prompt: "Use $flow-session-start when the user says Mulai, Start, or similar start-session commands for this repository."
policy:
  allow_implicit_invocation: true
